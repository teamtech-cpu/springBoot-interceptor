/*
package com.interceptor.interceptor.controller;

import com.interceptor.interceptor.TimerInterceptor;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurerAdapter;

@Component
public class TimerInterceptorService extends WebMvcConfigurerAdapter {
    @Autowired
    TimerInterceptor timerInterceptor;

    @Override
    public void addInterceptor(InterceptorRegistry registry){
        registry.addInterceptor(timerInterceptor);

    }
}
*/
